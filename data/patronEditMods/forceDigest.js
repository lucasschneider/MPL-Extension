/*
 * notification: HTML checkbox input element which sends
 *   patron email notification
 * digestBox:    nottification's checkbox input option to
 *   send digests only
 */
function forceDigest(notification, digestBox) {
}
